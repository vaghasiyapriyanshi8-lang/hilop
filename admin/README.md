# Hilop Admin Dashboard

Next.js admin dashboard for operations, analytics, and product management.

## Run locally

1. Install dependencies: `npm install`
2. Start development server: `npm run dev`
3. Set `NEXT_PUBLIC_API_BASE_URL` in `.env`
4. Deploy to Vercel for production
